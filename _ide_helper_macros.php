<?php

declare(strict_types=1);

namespace Illuminate\Database\Eloquent\Factories {
    use Illuminate\Database\Eloquent\Collection as DatabaseCollection;
    use Illuminate\Database\Eloquent\Model;

    /**
     * @method static DatabaseCollection<int, Model>|Model|null createFresh(callable|array<string, mixed> $attributes = [], ?Model $parent = null) Create a model (or models) and immediately reload it from the database, avoiding a separate call to fresh().
     */
    class Factory {}
}
